# Independent audit — Harry 12 July 2026 `multistep_v2` rebuild

## Verdict: PASS (local-only; no publish / upload / Index / remote write performed)

This audit was run by a separate process from the build. It re-examines the
rebuilt deck against the **frozen, passed revision guide** and the **current UI
standard** (the audited-PASS contract from 26 June `downstream_v2`).

## 1. Build provenance & source integrity
- The deck was rebuilt from scratch by `build_cards.py` / `build_index.py` /
  `render.py`, porting the frozen guide verbatim. The pre-existing non-standard
  build (which used a non-accessible `onclick`+blur-span reveal) was quarantined
  to `_quarantine_oldsrc/` before the rebuild; nothing from it was reused.
- Frozen guide unchanged: SHA-256 of
  `guide/harry_2026-07-12_differentiation_trig_revision_guide.tex` is
  `c426eb21cbaaf8ad00158cb694c8b5e049c62cefd7867fd55c33d44d84dd6968`
  (identical before and after the build). The guide was **not** modified.

## 2. Current-UI-standard acceptance (the gap the old build failed)
The prior local build used a non-accessible `onclick` span that **started the
working visible and blurred it on click** — it did NOT meet the "REAL accessible
reveal/hide control (keyboard+pointer+aria)" bar. The rebuilt deck uses the
audited-PASS contract instead:

| Card | Parts | Carry-ins | Reveal buttons (= hidden panels) |
|---|---|---|---|
| Warmup_derivatives_identities | 3 | 0 | 3 |
| P3_Jun2024_Q1 | 3 | 2 | 3 |
| P3_Jun2024_Q2 | 2 | 1 | 2 |
| P3_Jun2024_Q3 | 1 | 0 | 1 |
| P3_Jun2024_Q4 | 4 | 3 | 4 |

- Every card has one native `<button type="button" class="reveal-control"
  aria-controls aria-expanded="false">` per working block, wired to a unique
  `<section class="work-panel" hidden>` panel. `aria-controls` ↔ panel `id`
  pairs all match.
- Grey work boxes are maskable inside the hidden panel; blue `.carry-in` boxes
  render **outside** the hidden panel so carry-ins stay visible while working is
  masked.

## 3. Functional browser audit (Playwright + system Chromium, 10 card/viewport records)
From `browser_audit.json` (renderer: `playwright 1.x + system Chromium`):

| Check | Result |
|---|---|
| MathJax v3 tex-svg renders (mjx-container count > 0) | PASS on all 10 records |
| Work panels start **collapsed** (`aria-expanded=false`, panel `hidden`) | PASS all 10 |
| Pointer reveal/hide flips `aria-expanded` truthfully | PASS all 10 |
| Keyboard (Enter reveal / Space hide) flips `aria-expanded` truthfully | PASS all 10 |
| No horizontal overflow desktop (1280) | PASS all 5 |
| No horizontal overflow mobile (390) | PASS all 5 |
| No image failures; export PNG anchor resolves same-dir | PASS all 5 |
| Export button overlap at mobile | none |
| Landing (index.html) desktop/mobile | no overflow, FAIL=False |

MathJax emit was checked **warnings-as-errors**: any console warning/error
mentioning MathJax/TeX/SVG aborts the run. No such warnings occurred.

## 4. Strict-clean generator contract
- **No C0 control bytes** (except LF) in any generated card — verified by
  `audit_clean.py` and by the render-time guard in `render.py`. No control-character
  restoration shim exists; a stray C0 is a hard compile failure.
- **Raw TeX** (not `\tfrac`-as-literal-text): the real-leak test (TeX commands
  appearing in visible text outside `\(...\)` / `\[...\]` delimiters) found
  **zero** leaks, and the vision spot-checks confirmed rendered maths, not source.
- **Banned phrases** (html2canvas, "Generated by", "Reveal this box", etc.):
  none present in any card.
- **No transient paths**: all artifacts land in the persistent `multistep_v2/`.

## 5. Content fidelity to the frozen guide (verbatim port)
Key final answers reproduced verbatim and confirmed present:

- Q1: `P=(5,10)`, `x=\tfrac52` (i.e. `x<5/2`), image `(7,30)` ✓
- Q2: `A=2, B=-1, C=6`, `44+6\ln 3` ✓
- Q3: **discussed only** — only the evidenced review + two intercepted points
  `(0,6)`, `(2,0)` recorded; no invented full solution ✓
- Q4: `a=4, b=2, c=-1`; `R=2\sqrt5` (exact, not `4.472`); `f_max=2\sqrt5-1`;
  second-smallest `x≈3.69` ✓

Q3 is correctly labelled "Discussed only — not re-worked" in both the card and
the landing, so the deck cannot contradict itself.

## 6. Marks / parts / carry-ins
- Per-part marks are **not** shown, consistent with the frozen guide's
  "no official mark scheme in the source set" status and the prior compliant
  decks. Each part header carries a status label (Solved / Discussed only).
- Carry-ins propagate exactly where the guide chains parts: Q1(b)←(a),
  Q1(c)←(a), Q2(b)←(a), Q4(b)←(a), Q4(c)(i)←(b), Q4(c)(ii)←(b).

## 7. Visual evidence
- Desktop PNGs (full-page): `Warmup_…png`, `P3_Jun2024_Q{1,2,3,4}.png`,
  `index.png`.
- Mobile PNGs (390px, full-page): `.mobile_qa/*.png`.
- Vision spot-checks of `P3_Jun2024_Q4.png` (desktop) and
  `.mobile_qa/P3_Jun2024_Q1.png` (mobile) confirmed clean rendering, readable
  maths, visible diagrams, no clipping/overflow.

## 8. Scope / non-actions
This is a local rebuild + independent audit. **No** publish, **no** upload,
**no** Index/Notion/remote write was performed. The deck lives only in
`lessons/2026-07-12_harry_differentiation_trig/multistep_v2/`.

## Files
- Generator: `build_cards.py`, `build_index.py`, `render.py`, `diagrams.py`
- Audit: `audit_clean.py`, `browser_audit.json`
- Output: 5 card HTML + `index.html`, 5 card PNG + `index.png` + `.mobile_qa/*.png`,
  4 diagram PNGs, self-contained `mathjax-tex-svg.js` (kept from prior asset).
